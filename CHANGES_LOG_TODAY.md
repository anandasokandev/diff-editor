# Project Change Log - April 17, 2026

Summary of all architectural and functional changes implemented today in the Craftly Editor.

## 1. Canvas Export & Data Enrichment
Enhanced the export functionality to provide more detailed information in the console for database preparation.

- **File:** `src/app/services/canvas.service.ts`
    - **Change:** Modified `getCanvasState()` (Line 92) to `public`.
    - **New Method:** Added `getLayerName(el: any)` (Lines 130-134) to centralize item naming.
    - **New Method:** Added `exportCanvasJSON()` (Lines 137-156) which produces an enriched JSON with `width`, `height`, `layerName`, and `content` for every element.
- **File:** `src/app/components/topbar/topbar.component.ts`
    - **Method:** `download()` (Lines 47-58) now logs the enriched JSON to the console.
- **File:** `src/app/components/sidebar/sidebar.component.ts`
    - **Method:** `getLayerName()` (Lines 139-141) now calls the service-level helper to ensure UI consistency.

## 2. MSSQL Database Design
- **New File:** `DATABASE_DESIGN.md`
    - Created a comprehensive documentation for storing canvas states in Microsoft SQL Server, including table schemas, `ISJSON` constraints, and `JSON_VALUE` query examples.

## 3. AI Image Generation (URL Handling)
Refactored the AI service to support external image hosting instead of bulky base64 strings.

- **File:** `src/app/services/ai.service.ts`
    - **Method:** `generateImage(prompt, w, h)` (Lines 86-103)
        - Updated `http.post` to use `{ responseType: 'text' }` to handle raw URL responses from the backend.
        - Added logic to sanitize and validate incoming image URLs (including an `https` check).
        - Integrated multiple debug logs during the troubleshooting phase and subsequently cleaned them for production.

## 4. UI/UX & Code Polishing
- **File:** `src/app/components/ai-modal/ai-modal.component.ts`
    - Refined the `generate()` method to manage multiple image generations from AI layout suggestions.
    - Added and removed debugging breadcrumbs to verify the AI design pipeline.

---
**Report generated by Antigravity AI Engineering.**
